APP_PORT=3000
APP_HOST=0.0.0.0

# Optional: environment config has priority over the local settings file.
HOLYRICS_BASE_URL=
HOLYRICS_TOKEN=
# Optional: override where the server stores settings saved from the UI.
HOLYRICS_CONFIG_FILE=.holyrics-config.json
